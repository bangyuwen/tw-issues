## 1. Structural Contracts

- [x] 1.1 Update Hsinchu model tests so Chapter 01 targets `#primary-document-reading` first, retains the remaining downstream links in order, and omits both primary-document destinations for sparse or non-Hsinchu projections.
- [x] 1.2 Add rendered-HTML tests for `#main-content` → `#primary-document` → reading legend → `#case-contents` → `#primary-document-reading` → `#context` → `#responsibility-lines` → `#coverage-limits` order.
- [x] 1.3 Add rendered ownership assertions proving that gateway provenance, document coverage, metadata, and canonical actions occur once while every guide, excerpt, layer, proof boundary, limitation, and non-conclusion remains present once.
- [x] 1.4 Add heading, fragment-target, ARIA-ID, and generic-topic regression assertions for the new two-section structure.

## 2. Hsinchu Renderer And Navigation

- [x] 2.1 Split the eligible primary-document presentation into a top-level `#primary-document` source gateway and Chapter 01 `#primary-document-reading` detailed guide without changing the public projection or source eligibility rules.
- [x] 2.2 Make the gateway the sole owner of the existing title, exact warning, document coverage, source metadata, canonical Threads link, and `#source-58` link; add an ordinary forward link to the detailed guide.
- [x] 2.3 Keep the guide, checked excerpts, three evidence layers, proof boundaries, limitations, and non-conclusions in the downstream reading section; add a visible non-duplicating return reference to the gateway.
- [x] 2.4 Move dossier-wide `#coverage-limits` into Chapter 01 after context, give it an enclosing-level heading, and update the Hsinchu chapter descriptor and hero signposting to match the new source-first order.

## 3. Scoped Presentation And Accessibility

- [x] 3.1 Add Hsinchu-scoped source-gateway measure, gutters, spacing, and responsive rules while preserving the detailed guide's readable chapter measure and existing design tokens.
- [x] 3.2 Remove red rectangular fragment decoration from `#primary-document` and `#primary-document-reading`, preserve unobscured native fragment positioning, and leave all interactive `:focus-visible` indicators intact.
- [x] 3.3 Verify sequential h1–h6 structure, unique `aria-labelledby` relationships, minimum 44-by-44 CSS-pixel targets, wrapping, and one semantic DOM without client state or hover-only behavior.

## 4. Verification And Review

- [x] 4.1 Run the focused Hsinchu model and rendered-HTML tests, then inspect the fixed diff for unintended factual, source, anchor, or cross-topic changes.
- [x] 4.2 Run `npm test`, `npm run lint`, `npm run build`, and `npm run build:github-pages`; verify public output still excludes local images, manifests, transcripts, private producer data, secrets, and deployment identifiers.
- [x] 4.3 Inspect local rendered/runtime output at 390px, 799px, 800px, 801px, desktop, and a 200-percent-zoom equivalent for hierarchy, overflow, contrast, keyboard focus, direct fragments, Back/Forward, reduced motion, and JavaScript-disabled readability.
- [x] 4.4 Dispatch a fresh registered `independent-reviewer` against the exact fixed diff, resolve every blocking finding, and rerun only the validation invalidated by any final patch.
- [x] 4.5 Record the final local revision, changed files, command results, visual evidence, review outcome, residual risks, and the next external action that still requires explicit authorization.

## 5. Coverage Chronology Correction

- [x] 5.1 Rewrite only the first Hsinchu dossier-wide `gap` and `gapReason` so they have distinct roles, place the publicly reported non-prosecution disposition before the later stated reconsideration intent, and preserve every official-document and procedural limitation.
- [x] 5.2 Add model and rendered-HTML assertions for exact distinct field content, non-prosecution-before-reconsideration order, and absence of the duplicated legacy paragraph.
- [x] 5.3 Rerun the required test, lint, build, GitHub Pages, and narrow-screen rendered checks; then dispatch the registered `independent-reviewer` against the updated exact diff.

## 6. Disposition Evidence-Role Priority

- [x] 6.1 Update the Hsinchu projection so page-visible disposition content uses `source-58` as its first direct basis, while Threads remains only the publication channel and media remains attributed reaction or procedure.
- [x] 6.2 Reconcile mixed claims so content outside the visible page scope is not implied to come from `source-58`; split or narrow proof scope where necessary.
- [x] 6.3 Put `source-58` first in the first coverage-gap references, retain an official source for disposition occurrence, and keep reconsideration reporting explicitly short of filed or accepted status.
- [x] 6.4 Add model and rendered-HTML regressions for source priority, page scope, channel/media separation, and reconsideration non-promotion.
- [x] 6.5 Rerun affected validation and a fresh registered independent-reviewer gate on the exact revised diff.
